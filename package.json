{
  "name": "qianshou",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "description": "A local-first AI software delivery control plane.",
  "scripts": {
    "dev": "pnpm --filter @qianshou/core build && pnpm -r --parallel dev",
    "build": "pnpm --filter @qianshou/core build && pnpm --filter @qianshou/web build && pnpm --filter @qianshou/server build",
    "start": "pnpm --filter @qianshou/server start",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=22"
  }
}
