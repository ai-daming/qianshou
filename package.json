{
  "name": "qianshou",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "description": "A local-first AI software delivery control plane.",
  "scripts": {
    "dev": "pnpm --filter @qianshou/core build && pnpm -r --parallel dev",
    "build": "pnpm --filter @qianshou/api-client build && pnpm --filter @qianshou/core build && pnpm --filter @qianshou/web build && pnpm --filter @qianshou/server build",
    "start": "pnpm --filter @qianshou/server start",
    "api:generate": "pnpm --filter @qianshou/api-client generate",
    "api:check": "node scripts/check-api-client.mjs",
    "test": "pnpm -r test",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm api:check && pnpm format:check && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=22"
  }
}
